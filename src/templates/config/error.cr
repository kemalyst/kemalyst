Kemalyst::Handler::Error.config do |config|
  # set the path under `src/views` where the error templates will reside.
  # The Error Handler will look for templates named 403, 404 or 500.
  # config.error_path = "error"

  # specify which template engine to use. defaults to `ecr`.
  # config.template_type = "ecr"
end
